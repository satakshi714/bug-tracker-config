server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bug_tracker_auth
    username: root
    password: shinchan
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: "change-this-to-a-long-random-secret-key-for-development-only"
  expiration: 86400000